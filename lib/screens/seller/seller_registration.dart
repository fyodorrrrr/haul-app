import 'package:flutter/material.dart';

class seller_registration extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: "Seller Page"
      home: Scaffold (
        body: Stack(
          
        )
      )

    )
  }
}